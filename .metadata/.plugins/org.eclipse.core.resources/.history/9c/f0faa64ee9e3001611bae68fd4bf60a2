package movies;

import java.util.ArrayList;

public class Actor {
	private String name;
	private ArrayList<Movie> movies;

	public Actor(String name) {
		this.name = name;
		this.movies = new ArrayList();
	}
	
	public boolean actedIn(Object movie){
		if (movie instanceof Movie);
			for (Object aMovie : this.movies){
				if (aMovie.equals(movie)) {
					return true;
				}
			}
		return false;
	}
	
	public boolean actedInSameMovies(Actor actor){

		for (Object movie : this.movies){
			if (actor.actedIn(movie)){
				return true;
				}
			}
		return false;	
	}			
	
	public boolean actedWith(Object actor){
		if (actor instanceof Actor) {
			for (Object movie : actor.movies) {
				if (movie in this.movies)
// finish with equals to instead of in

	public void addMovie(Movie movie){
		this.movies.add(movie);
	}
	public boolean equals(Object object){
		if (other instance Actor){
			other
	public String getName(){
		return this.name
		}
	public toString().
}
